name: remind_ai
description: "REMind-Ai — AI-powered dream interpretation across four unique styles powered by Google Gemini."
publish_to: 'none'

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # State management
  flutter_riverpod: ^3.1.0
  riverpod: ^3.1.0
  riverpod_annotation: ^4.0.0

  # Routing
  go_router: ^17.0.1

  # Network
  dio: ^5.4.0

  # Local storage
  hive_ce: ^2.11.1
  hive_ce_flutter: ^2.3.0
  flutter_secure_storage: ^9.2.4

  # Models
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0

  # Functional programming
  fpdart: ^1.1.0

  # Theme & design
  google_fonts: ^8.0.2
  flutter_animate: ^4.5.2

  # Utils
  gap: ^3.0.1
  intl: ^0.20.2
  path_provider: ^2.1.5
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  build_runner: ^2.10.4
  json_serializable: ^6.9.4
  riverpod_generator: ^4.0.0
  riverpod_lint: ^3.1.0
  freezed: ^3.2.3
  hive_ce_generator: ^1.9.1
  flutter_launcher_icons: ^0.14.4
  msix: ^3.16.8

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  uses-material-design: true

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

  assets:
    - assets/icon/

# Microsoft Store / MSIX package configuration (Windows).
# Build with: dart run msix:create
msix_config:
  display_name: REMind-Ai
  publisher_display_name: Liviu-Stefan Petroaia
  # NOTE: MSIX identity names allow only letters, digits, periods and
  # hyphens (no underscores), so this intentionally differs from the
  # Android applicationId `com.remindai.remind_ai`.
  identity_name: com.remindai.remindai
  # Required for Microsoft Store submission. Reserve the app name in
  # Partner Center, then paste the assigned Publisher Identity here, e.g.
  #   publisher: CN=ABCD1234-1234-1234-1234-1234567890AB
  # Leave commented for local/sideload builds (msix self-signs).
  # publisher: CN=<from-partner-center>
  msix_version: 1.0.0.0
  capabilities: 'internetClient'
  logo_path: assets/icon/icon.png
  architecture: x64
  store: false
